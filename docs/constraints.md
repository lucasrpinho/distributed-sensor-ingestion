# Constraints

- Runs **locally only** using Docker / Docker Compose
- Single Git repository (monorepo)
- Minimal UI (optional; not required)
- Focus on architectural clarity over feature completeness
- Avoid unnecessary abstractions

---
